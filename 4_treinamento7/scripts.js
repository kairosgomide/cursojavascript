/* Faça um programa que calcule a área de um triângulo. O usuário deve ler a base e a altura.*/

let base = parseInt(prompt("Digite a base de um triângulo"));
let altura = parseInt(prompt("Digite a altura de um triângulo"));

let area = base * altura / 2;

console.log(`A area do triângulo é ${area}`);
